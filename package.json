{
  "name": "culinaire-kitchen",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered platform for chefs, restaurateurs, and culinary professionals",
  "scripts": {
    "predev": "node scripts/kill-dev-ports.mjs",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "tsc:check": "turbo run tsc:check",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3",
    "turbo": "^2",
    "typescript": "^5.7"
  },
  "packageManager": "pnpm@10.31.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "esbuild",
      "tesseract.js"
    ]
  }
}
