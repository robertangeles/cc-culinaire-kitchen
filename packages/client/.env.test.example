# Credentials for the Playwright E2E suite. Copy to .env.test and fill in.
# .env.test is gitignored — never commit real credentials.
E2E_USER_EMAIL=
E2E_USER_PASSWORD=

# Optional override — defaults to http://localhost:5179 (matches CLAUDE.md ports).
# E2E_BASE_URL=http://localhost:5179
