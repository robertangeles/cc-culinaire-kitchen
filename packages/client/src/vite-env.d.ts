/// <reference types="vite/client" />

/**
 * Build-time constant injected by Vite's `define` option.
 * Value is read from the monorepo root `package.json` version field.
 */
declare const __APP_VERSION__: string;
