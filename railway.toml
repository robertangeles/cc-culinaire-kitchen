[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm run build && pnpm --filter @culinaire/server db:push && pnpm --filter @culinaire/server db:seed"

[deploy]
startCommand = "node packages/server/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
